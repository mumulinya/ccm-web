# 侧边栏品牌区优化

## 变更

- 将临时感较强的 `cc-web + v1.0.0` 文本区改为 CCM 品牌标识。
- 增加 `Agent Workspace` 产品副标题，并弱化版本号层级。
- 品牌标识使用现有 Lucide 图标和主题变量，兼容深色与浅色模式。
- 点击品牌区返回“我的工作台”。
- 删除顶部直接跳转菜单管理页面的快捷按钮。
- 菜单管理页面及系统菜单入口仍保留，不影响已有导航配置能力。

## 验证

- 前端生产构建通过。
- 侧边栏专项自测通过。
- 浏览器检查品牌文本、首页跳转和菜单管理快捷按钮移除状态。
- 付费模型调用：`0`。
