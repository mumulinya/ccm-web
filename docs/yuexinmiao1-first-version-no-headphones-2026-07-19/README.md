# 月薪喵第一版音乐动作去耳机

## 修改

- 保留已恢复的第一版月薪喵图集、动作映射和其余 18 个状态。
- 删除音乐动作的头梁和左右耳罩。
- 保留原角色逐帧律动，并用黄色音符和节奏线表达音乐状态。
- 不修改用户宠物配置，也不启动桌宠。

## 验证

- 重新生成 19 个状态到前端、运行时和 Electron 桌宠资源目录。
- 资源自测增加“音乐动作不包含第一版耳机结构”回归检查。
- 浏览器确认音乐动作没有耳机，角色完整显示，控制台错误为 `0`。
- 付费模型调用为 `0`。
