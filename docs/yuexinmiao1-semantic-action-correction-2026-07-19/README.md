# 月薪喵动作语义纠正

> 状态：后续实验记录，不是当前实现。当前已恢复第一次替换版本。

## 问题

第一版替换保留了 `yuexinmiao1` 的外观，但部分 CCM 状态只重新编排原图集帧，没有真正修改动作内容，导致完成状态可能显示惊讶或警报，音乐状态可能显示拿剑，资源中心还展示了不存在的旧图集路径。

## 修正

- 除待机和拖拽外，19 个业务状态全部绑定独立 SVG。
- 思考、规划、工作、执行、调试和验收分别增加笔记、清单、代码、进度、错误目标和检查清单。
- 音乐改为无耳机的身体律动，整理上下文增加扫帚，搬运使用独立绘制的资料箱动作。
- 提醒增加铃铛，完成增加笑脸与绿色完成勾，成功增加庆祝表情，错误增加错误面板。
- 打哈欠、犯困、入睡过渡、睡觉和醒来分别使用不同面部、姿态与被子变化。
- 动画资源中心读取内置皮肤的真实 `spritesheetPath` 和补充 SVG 路径，不再显示 `generated/yuexinmiao/spritesheet.webp` 或错误的资源缺失状态。

## 验证

- 浏览器实际检查音乐、整理上下文、完成/开心、规划和睡眠画面，动作内容与状态标签一致。
- 动画资源中心显示 `yuexinmiao1/spritesheet.webp` 和 `yuexinmiao1/planning.svg`，旧路径不存在，资源缺失数量为 `0`。
- 浏览器控制台错误为 `0`。
- `node scripts/yuexinmiao1-replacement-selftest.mjs`：通过，19 个补充动作和来源图集校验通过。
- `npm run build:frontend`、`npm run check`：通过。
- 未启动桌宠、未修改用户的宠物配置，付费模型调用为 `0`。

后续已在 `docs/yuexinmiao1-standalone-svg-actions-2026-07-19/README.md` 完成纯 SVG 重绘；本文件记录的早期补充动作实现不再是当前资源形态。
