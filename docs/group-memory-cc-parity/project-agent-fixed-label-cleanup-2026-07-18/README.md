# 项目 Agent 固定标签精简

## 结果

- 项目会话的 Agent 回复不再重复展示固定“项目 Agent”标签。
- 用户和 Agent 的可配置头像继续用于区分消息双方。
- 项目任务运行中的状态提示、输入框文案和项目管理操作名称保持不变。
- 群聊中的具体项目 Agent 名称继续保留，用于区分不同执行成员。

## 验证

- 前端 production build 通过。
- 已有项目会话中的固定标签数量为 0。
- 浏览器控制台无新增 error。
