# 群聊用户消息默认路由标签精简

## 结果

- 用户消息默认发送给群聊主 Agent 时，不再展示“用户 -> @协调者”。
- `all` 或缺省目标同样不展示默认路由标签。
- 用户明确发送给项目 Agent 时，继续展示目标 Agent 名称。
- 本次变更只影响消息展示，不改变消息目标、路由或任务分派。

## 验证

- 前端 production build 通过。
- 本地已有群聊会话中的默认协调者路由标签不可见。
- 浏览器控制台无 error 日志。
