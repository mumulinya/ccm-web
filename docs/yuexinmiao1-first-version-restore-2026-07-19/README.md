# 月薪喵第一版恢复

## 原因

后续的纯手绘角色方案和来源帧加新道具方案，实际观感均不如第一次替换版本。根据用户要求，停止继续调整后两套方案，恢复第一次替换完成后的稳定快照。

## 恢复内容

- 从当前任务日志恢复 `build-yuexinmiao1-supplemental.mjs` 的第一版最终内容，不凭记忆重写。
- 保留第一版使用的 8×9 来源 WebP 图集、逐帧编排和业务覆盖层。
- 保留第一版后续已经修好的三目录资源同步、SVG 内嵌图集、防外部子资源空白和睡眠遮挡修复。
- 思考、规划、工作、执行、调试、音乐、整理、提醒、完成和睡眠等状态恢复第一版画面与节奏。
- 后续纯手绘 SVG 和来源角色合成方案不再生成，也不再作为当前实现。

## 验证

- 生成器成功输出 19 个状态到 3 个资源目录。
- `node scripts/yuexinmiao1-replacement-selftest.mjs`：通过。
- `npm run build:frontend`：通过。
- `npm run check`、目标资源 `git diff --check`：通过。
- 浏览器检查思考、规划、音乐和睡眠，画面均为第一版资源，控制台错误为 `0`。
- 未启动桌宠，未修改用户宠物配置，付费模型调用为 `0`。
