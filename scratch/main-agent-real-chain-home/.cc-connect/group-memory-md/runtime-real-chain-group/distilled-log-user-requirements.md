---
name: "Distilled group-log user requirements"
description: "Long-term user constraints and goals distilled from the group transcript."
type: "user"
source: "auto:group-log-distillation"
paths: []
group_id: "runtime-real-chain-group"
updated_at: "2026-07-12T04:05:50.901Z"
checksum: "3853aef8ef3f4bdc1aced54f"
---

# Distilled Group-Log User Requirements

Generated by CCM long-term group-log distillation at 2026-07-12T04:05:50.901Z.
Each fact keeps its source message id so a future agent can recover the raw transcript before trusting file/function/flag claims.

## Distilled Facts
- #runtime-real-chain-group-real-chain-1783829085375 [requirement] 用户 -> coordinator: 请在 runtime-e2e-project 完成一次真实代码交付。
把 src/feature.js 的 deliveryMarker 修改为 group-real-chain-1783829085375。
必须实际运行 npm test 和 npm run build。
完成后必须交给 TestAgent 独立复核，并由主 Agent 抽查后再总结。
- #mmrh9s9pptask [requirement] 用户 -> coordinator: 【主 Agent 业务开发工作单】
任务标题：请在 runtime-e2e-project 完成一次真实代码交付。
业务目标：请在 runtime-e2e-project 完成一次真实代码交付。 把 src/feature.js 的 deliveryMarker 修改为 group-real-chain-1783829085375。 必须实际运行 npm test 和 npm run build。 完成后必须交给 TestAgent 独立复核，并由主 Agent 抽查后再总结。
验收标准：主 Agent 必须完成需求理解、计划、子 Agent 分派、执行跟踪、必要返工和最终验收。 涉及代码的任务必须提供实际文件变更和已执行的构建或测试证据。 最终报告必须说明完成内容、变更文件、验证结果、风险以及仍需用户处理的事项。
关联文档：用户原始开发指令： 请在 runtime-e2e-project 完成一次真实代码交付。 把 src/feature.js 的 deliveryMarker 修改为 group-real-chain-1783829085375。 必须实际运行 npm test 和 npm run build。 完成后必须交给 TestAgent 独立复核，并由主 Agent 抽查后再总结。
完整任务说明：
业务目标：
请在 runtime-e2e
...[typed-memory truncated]...
 Agent。
- 每个被派发的子 Agent 必须拿到明确的实现范围、文件/模块方向、验收标准和风险提示。
- 子 Agent 必须返回 CCM_AGENT_RECEIPT；缺回执、缺证据或状态不是 done 时不能判定完成。
- 完成门禁：必须有系统实际捕获的代码/配置/文档文件变更。
- 验证门禁：必须有可采信的已执行验证记录；只写建议运行、未运行或失败验证不能完成。
- 主 Agent 必须等待子 Agent 完成并复盘；发现缺口时继续返工或向用户明确索要信息。
- 最终报告必须说明完成内容、涉及项目/文件、已执行验证、风险、阻塞和仍需用户确认的事项。
