/** Product-level reply for capability questions. It deliberately bypasses the model. */
export declare const GLOBAL_AGENT_CAPABILITY_REPLY: string;
export declare function isGlobalAgentCapabilityQuestion(value: any): boolean;
