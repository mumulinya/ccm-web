# CCM Frontend

Vue 3 and Vite source for the CCM web interface.

From the repository root, run `npm run dev:frontend` for development or
`npm run build:frontend` to write the production assets to
`ccm-package/public`.
